# v0.1.1-supabase-schema Release Notes

Date: 2026-05-09

Tag target commit: `6540f1f`

## Summary

This checkpoint adds the planned Supabase/PostgreSQL schema and Row Level Security baseline for the AegorynOS MVP.

## Added

- `0001_initial_schema.sql` migration for core MVP tables and indexes.
- `0002_rls_policies.sql` migration for user-owned Row Level Security policies.
- Supabase seed-data README.
- Generic local seed template.
- Supabase setup guide.

## Changed

- Database schema documentation now points to migration files as the authoritative schema source.
- MVP backlog now tracks Supabase schema and seed-file progress.

## Security

- Added RLS policies for:
  - users_profile;
  - accounts;
  - money_buckets;
  - transactions;
  - projects;
  - tasks;
  - ai_messages;
  - ai_usage.

## Verification

- Migration files and docs were committed.
- Runtime Supabase execution was still a following checkpoint.

## Known Limitations

- Migrations were not yet verified against the hosted Supabase project at this tag.
- No dashboard data connection yet.
- No login page yet.

## Rollback

Use this tag to inspect the Supabase schema and RLS file baseline:

```powershell
git checkout v0.1.1-supabase-schema
```
